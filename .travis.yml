language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t raminblair/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true raminblair/docker-react npm run test -- --coverage